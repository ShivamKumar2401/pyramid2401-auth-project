<!DOCTYPE html>
<html>
<head>
    <title>Create Account</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', sans-serif;
            background: url("https://images.unsplash.com/photo-1521790360280-8c3a1551f7f0")
                        no-repeat center center/cover;
            position: relative;
            overflow: hidden;
        }

        /* Light overlay */
        body::before {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(255, 255, 255, 0.65);
            backdrop-filter: blur(3px);
        }

        .main-container {
            position: relative;
            z-index: 2;
            width: 900px;
            height: 550px;
            display: flex;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
        }

        .left-side {
            width: 45%;
            background: #e07a5f;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 40px;
        }

        .left-side h2 {
            font-weight: 700;
        }

        .right-side {
            width: 55%;
            background: #1d3557;
            color: white;
            padding: 50px;
        }

        .right-side h2 {
            margin-bottom: 30px;
            font-weight: 600;
            color: #f4a261;
        }

        .form-control {
            border-radius: 6px;
            margin-bottom: 20px;
        }

        .btn-custom {
            background: #f4a261;
            border: none;
            color: white;
            font-weight: 600;
        }

        .btn-custom:hover {
            background: #e76f51;
        }

        .login-link {
            margin-top: 15px;
            font-size: 14px;
        }

        .login-link a {
            color: #f4a261;
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }

        /* Decorative Shapes */
        .shape1 {
            position: absolute;
            top: 60px;
            left: 80px;
            width: 120px;
            height: 120px;
            background: #f4a261;
            border-radius: 30% 70% 60% 40% / 40% 30% 70% 60%;
            opacity: 0.3;
            z-index: 1;
        }

        .shape2 {
            position: absolute;
            bottom: 80px;
            right: 120px;
            width: 150px;
            height: 150px;
            background: #1d3557;
            border-radius: 50%;
            opacity: 0.2;
            z-index: 1;
        }

    </style>
</head>

<body>

<div class="shape1"></div>
<div class="shape2"></div>

<div class="main-container">

    <!-- Left Side -->
    <div class="left-side">
        <div>
            <h2>Let’s Manage<br>Your Daily Products!</h2>
            <p class="mt-3">
                Create your account and start organizing your daily use items easily.
            </p>
        </div>
    </div>

    <!-- Right Side -->
    <div class="right-side">

        <h2>Create An Account</h2>

        {% if error %}
        <div class="alert alert-danger">
            {{ error }}
        </div>
        {% endif %}

        <form method="POST">

            <!-- Username -->
            <input type="text"
                   name="username"
                   class="form-control"
                   placeholder="Username"
                   required>

            <!-- Email -->
            <input type="email"
                   name="email"
                   class="form-control"
                   placeholder="Email Address"
                   required>

            <!-- Password -->
            <input type="password"
                   name="password"
                   class="form-control"
                   placeholder="Password"
                   required>

            <!-- Confirm Password -->
            <input type="password"
                   name="confirm_password"
                   class="form-control"
                   placeholder="Confirm Password"
                   required>

            <button type="submit" class="btn btn-custom w-100">
                Create Account
            </button>

        </form>

        <div class="login-link">
            Already have an account?
            <a href="{{ request.route_url('login') }}">Login here</a>
        </div>

    </div>

</div>

</body>
</html>
